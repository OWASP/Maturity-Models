doctype html
html(ng-app = "MM_Graph")
    head
        meta(charset="utf-8")
        meta(name="viewport", content="width=device-width")

        link(rel="stylesheet", href="/lib/foundation/css/foundation.min.css")

        script(src='/lib/angular/angular.js')
        script(src='/lib/angular-route/angular-route.js')
        script(src='/ui/js/angular-src.js')

        script(src="/lib/jquery/dist/jquery.js")
        script(src="/lib/foundation/js/foundation.js")

        base(href='/view')

    style.
        .fullWidth {
            width: 100%;
            margin-left: auto;
            margin-right: auto;
            max-width: initial;
        }
    body
        include ./includes/top-banner.pug
        block content
        script.
            $(document).foundation();